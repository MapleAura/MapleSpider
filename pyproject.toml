[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crawler_framework"
version = "0.1.0"
description = "Modular crawler framework with XHS plugin"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["crawler_framework"]
